# Config temporária apenas para validação do certificado SSL (primeira vez)
server {
    listen 80;
    server_name _;
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    location / { return 200 'ok'; }
}
