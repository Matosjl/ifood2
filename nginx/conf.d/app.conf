# =============================================================
# app.conf — HTTP phase (before SSL is initialised)
#
# After running:  ./scripts/init-ssl.sh <domain> <email>
# this file is replaced with the full HTTP + HTTPS config.
# =============================================================

# ── Upstreams ─────────────────────────────────────────────────
# frontend usa upstream normal (reinicia raramente)
upstream frontend { server frontend:80; }

# ── HTTP server ───────────────────────────────────────────────
server {
    listen 80;
    server_name _;

    # Docker DNS — re-resolve backend a cada 10s (evita IP stale após restart)
    resolver 127.0.0.11 valid=10s ipv6=off;
    resolver_timeout 5s;

    # Health check interno do nginx
    location = /health {
        access_log off;
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }

    # Let's Encrypt ACME webroot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # ── Socket.io (WebSocket) ─────────────────────────────────
    location /socket.io/ {
        set $backend http://backend:3000;
        proxy_pass         $backend;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection $connection_upgrade;
        proxy_set_header   Host       $host;
        proxy_set_header   X-Real-IP  $remote_addr;
        proxy_read_timeout 86400s;
        proxy_cache_bypass $http_upgrade;
    }

    # ── REST API ──────────────────────────────────────────────
    location /api/ {
        limit_req  zone=api  burst=20 nodelay;
        set $backend http://backend:3000;
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Connection         "";
        proxy_set_header Host               $host;
        proxy_set_header X-Real-IP          $remote_addr;
        proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto  $scheme;
        proxy_connect_timeout 10s;
        proxy_send_timeout    30s;
        proxy_read_timeout    30s;
    }

    # ── Register — sem rate limit (protegido pela ADMIN_KEY) ────
    location = /api/auth/register {
        set $backend http://backend:3000;
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Connection         "";
        proxy_set_header Host               $host;
        proxy_set_header X-Real-IP          $remote_addr;
        proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto  $scheme;
    }

    # ── Login — rate limit apertado (anti brute-force) ────────
    location = /api/auth/login {
        limit_req  zone=auth  burst=5 nodelay;
        set $backend http://backend:3000;
        proxy_pass $backend;
        proxy_http_version 1.1;
        proxy_set_header Connection         "";
        proxy_set_header Host               $host;
        proxy_set_header X-Real-IP          $remote_addr;
        proxy_set_header X-Forwarded-For    $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto  $scheme;
    }

    # Static uploads (product images)
    location /uploads/ {
        alias /var/www/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
        add_header X-Content-Type-Options nosniff;
        try_files $uri =404;
    }

    # ── Frontend SPA ──────────────────────────────────────────
    location / {
        proxy_pass         http://frontend;
        proxy_set_header   Host      $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_cache_bypass 1;
    }
}
